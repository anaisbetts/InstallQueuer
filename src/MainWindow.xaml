<Window x:Class="InstallQueuer.Ui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Width="350" Height="525">
    
    <Grid DataContext="{Binding Path=ViewModel, ElementName=MainWindow}">    
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <ListBox Margin="16"/>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="0">Gear</Button>
            <Button Grid.Column="1">Start!</Button>
            <Button Grid.Column="2">+</Button>
        </Grid>
    </Grid>
</Window>

<!-- vim: ts=4:sts=4:sw=4:et
-->
